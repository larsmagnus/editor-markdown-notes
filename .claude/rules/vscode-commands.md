---
paths:
  - 'package.json'
---

# VS Code Commands

- Adding, removing, or retitling a `contributes.commands` entry means updating the `## Commands` table in `README.md` in the same change
- Keep the table's command IDs, titles, and one-line purposes in sync with `package.json`
