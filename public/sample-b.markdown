# Sample .markdown file

This file uses the `.markdown` extension instead of `.md`, but opens in
exactly the same editor with exactly the same support: **bold**, _italic_,
and `inline code` all work here.

- One
- Two
- Three

> A blockquote, just to round things out.
